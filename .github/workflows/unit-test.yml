name: Run Unit Tests and Merge to Dev

on:
  push:
    branches: 
      - feature/**

jobs:
  test:
    name: Run Unit Tests
    uses: dariasmoises979-blip/pipeline-build/.github/workflows/test.yml@main

  merge-to-dev:
    name: Merge to Dev
    needs: test
    runs-on: ubuntu-latest
    if: success()
    permissions:
      contents: write
      pull-requests: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: true
      
      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
      
      - name: Merge to dev
        run: |
          git fetch origin dev
          git checkout dev
          git merge --no-ff ${{ github.ref }} -m "Auto-merge from ${{ github.ref_name }} after successful tests"
          git push origin dev
########################################3

  # Workflow: Crea automáticamente un Pull Request desde QA hacia Integration
  # cuando el merge a dev fue exitoso y las pruebas pasaron correctamente.

  create-pr:
    name: Create Pull Request for QA Manual Merge 
    needs: merge-to-dev                # Espera que el job "merge-to-dev" termine antes
    if: ${{ needs.test.result == 'success' }}  # Solo se ejecuta si el job "test" tuvo éxito
    runs-on: ubuntu-latest             # Se ejecuta en un runner Ubuntu

    steps:
    # Paso 1: Checkout del repositorio
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        ref: dev                       # Se asegura de usar la rama "dev"
        fetch-depth: 0                 # Obtiene todo el historial (necesario para PRs completos)

    # Paso 2: Obtener el SHA corto del último commit
    - name: Get commit short SHA
      id: vars
      run: echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
      # Guarda el SHA corto como variable para usarlo después en el título del PR

    # Paso 3: Crear el Pull Request de QA hacia Integration
    - name: Create Pull Request QA → Integration
      uses: peter-evans/create-pull-request@v6
      with:
        token: ${{ secrets.GH_TOKEN2 }}   # Usa un token con permisos para crear el PR
        base: integration                 # Rama destino del PR
        branch: qa                        # Rama origen del PR
        title: "QA Approval Needed - Commit ${{ steps.vars.outputs.sha_short }}"
        body: |                           # Mensaje que aparecerá dentro del PR
          🚀 Se creó este PR automáticamente desde **dev** hacia **qa** después de un merge exitoso desde feature.

          Commit asociado: `${{ steps.vars.outputs.sha_short }}`

          Por favor, revisa y aprueba este PR para continuar el flujo hacia **integration**.
        draft: false                      # El PR no es un borrador
        delete-branch: false              # No borra la rama "qa" después del PR
