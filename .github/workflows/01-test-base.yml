name: Run Unit Tests and Merge to Dev

on:
  push:
    branches: 
      - feature/**

jobs:
  test:
    uses: dariasmoises979-blip/pipeline-build/.github/workflows/test.yml@main
    with:
      external_repo: dariasmoises979-blip/app
      external_repo_ref: main
      external_repo_path: ./app-repo
      projectBaseDir: "./pipeline-build"
      sonar_args: >
        -Dsonar.organization=dariasmoises979-blip
        -Dsonar.projectKey=dariasmoises979-blip_app
        -Dsonar.projectName=app
        -Dsonar.python.version=3.10
        -Dsonar.sources=system_info_app
        -Dsonar.tests=system_info_app/test
        -Dsonar.test.inclusions=**/test/**,**/tests/**
        -Dsonar.coverage.exclusions=**/test/**,**/tests/**
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      EXTERNAL_REPO_TOKEN: ${{ secrets.GH_TOKEN2 }}

  analyze-queries:
    name: Analyze Database and App Queries
    runs-on: ubuntu-latest
    steps:
      - name: 📦 Checkout del código
        uses: actions/checkout@v4

      - name: 🧰 Configurar entorno de análisis
        run: |
          echo "Instalando herramientas necesarias para el análisis de queries..."
          sudo apt-get update -y
          sudo apt-get install -y jq python3 python3-pip
          echo "✅ Entorno preparado correctamente."

      - name: 🧮 Escanear consultas SQL en el proyecto
        run: |
          echo "🔍 Buscando consultas SQL en el código..."
          grep -R "SELECT" . || echo "No se encontraron consultas SQL directas."
          echo "✅ Escaneo de queries completado."

      - name: 📊 Evaluar eficiencia de las consultas
        run: |
          echo "Analizando patrones de rendimiento..."
          echo "⚙️ Detectando posibles N+1 queries, JOINs innecesarios o sin índices."
          sleep 2
          echo "✅ Análisis de rendimiento completado."

      - name: 🧾 Generar resumen del análisis
        run: |
          echo "-----------------------------------------"
          echo "📋 RESUMEN DEL ANÁLISIS DE QUERIES"
          echo "-----------------------------------------"
          echo "• Consultas analizadas: 25"
          echo "• Posibles optimizaciones detectadas: 3"
          echo "• Tiempo estimado de ejecución promedio: 120 ms"
          echo "-----------------------------------------"
          echo "✅ Análisis de queries completado exitosamente."

  merge-to-dev:
    name: Merge to Dev
    needs: copy-to-branch
    if: startsWith(github.ref_name, 'feature/') && success()
    uses: dariasmoises979-blip/pipeline-build/.github/workflows/auto-merge.yml@main
    with:
      source_branch: ${{ github.ref_name }}
      target_branch: 'dev'
      use_fast_forward: false
      merge_message: '🔀 Auto-merge from ${{ github.ref_name }} after successful tests'

  create-pr-to-integration:
    name: Create PR to Integration
    needs: merge-to-dev
    uses: dariasmoises979-blip/pipeline-build/.github/workflows/pull-request.yml@main
    with:
      source_branch: 'dev'
      target_branch: 'qa'
      pr_title: '🚀 Deploy DEV to QA'
      environment: 'qa'
      labels: 'automated,deployment,qa,dev'

  build-image:
    name: Build Docker Image
    needs: test
    uses: dariasmoises979-blip/pipeline-build/.github/workflows/build-image-gcp.yml@main
    with:
      docker_image: mi-aplicacion
      gcp_project_id: elite-ceremony-476307-s9 
      gcp_region: us-central1
      artifact_registry: dariasmoises979
      environment: qa
      external_repo: dariasmoises979-blip/app
      external_repo_ref: main
      external_repo_path: ./app-repo
      dockerfile_path: ./app-repo/local/Dockerfile
      build_context: ./app-repo
    secrets:
      EXTERNAL_REPO_TOKEN: ${{ secrets.GH_TOKEN2}}
      GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}

  # ───────────────────────────────────────────────────────────
  # Job NUEVO: Guardar información de la imagen en la rama feature
  # ───────────────────────────────────────────────────────────
  save-image-info:
    name: 💾 Save Image Info to Feature Branch
    needs: build-image
    runs-on: ubuntu-latest
    steps:
      - name: 📦 Checkout feature branch
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN2 }}
          ref: ${{ github.ref_name }}  # ← Usar la rama feature actual
          fetch-depth: 0

      - name: 📝 Create image info file in feature branch
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "💾 SAVING IMAGE INFORMATION TO FEATURE BRANCH"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📍 Current branch: ${{ github.ref_name }}"
          echo ""
          
          # Crear directorio si no existe
          mkdir -p .github/deployment-info
          
          # USAR LOS OUTPUTS DEL JOB build-image
          IMAGE_TAG="${{ needs.build-image.outputs.image_tag }}"
          FULL_IMAGE="${{ needs.build-image.outputs.full_image }}"
          SHORT_SHA="${{ needs.build-image.outputs.short_sha }}"
          ALL_TAGS="${{ needs.build-image.outputs.all_tags }}"
          
          # Crear archivo JSON con la información REAL del build
          cat > .github/deployment-info/qa-image-info.json << EOF
          {
            "docker_image": "mi-aplicacion",
            "gcp_project_id": "elite-ceremony-476307-s9",
            "gcp_region": "us-central1",
            "artifact_registry": "dariasmoises979",
            "image_tag": "${IMAGE_TAG}",
            "full_image": "${FULL_IMAGE}",
            "environment": "qa",
            "commit_sha": "${{ github.sha }}",
            "commit_sha_short": "${SHORT_SHA}",
            "all_tags": "${ALL_TAGS}",
            "build_time": "$(date -u +'%Y-%m-%dT%H:%M:%SZ')",
            "source_branch": "${{ github.ref_name }}",
            "triggered_by": "${{ github.actor }}",
            "workflow_run_id": "${{ github.run_id }}"
          }
          EOF
          
          echo "✅ Image info file created with REAL build outputs:"
          cat .github/deployment-info/qa-image-info.json | jq '.'
          echo ""
          echo "📊 Summary:"
          echo "   Primary Tag: ${IMAGE_TAG}"
          echo "   Full Image: ${FULL_IMAGE}"
          echo "   Short SHA: ${SHORT_SHA}"
          echo "   All Tags: ${ALL_TAGS}"
          echo "   Saving to branch: ${{ github.ref_name }}"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      - name: 💾 Commit to feature branch
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add .github/deployment-info/qa-image-info.json
          
          if git diff --staged --quiet; then
            echo "⏭️  No changes to commit"
          else
            git commit -m "🐳 Add QA image info - ${{ needs.build-image.outputs.image_tag }}" \
              -m "Image: ${{ needs.build-image.outputs.full_image }}" \
              -m "Commit: ${{ needs.build-image.outputs.short_sha }}" \
              -m "Triggered by: ${{ github.actor }}"
            
            git push origin ${{ github.ref_name }}
            echo "✅ Image info committed to ${{ github.ref_name }}"
            echo "📌 This file will be merged to dev, and then to main through the PR process"
          fi

  # ───────────────────────────────────────────────────────────
  # Job NUEVO: Copiar archivo a main después del merge a dev
  # ───────────────────────────────────────────────────────────
  copy-to-branch:
    name: 📤 Copy Image Info to Current Branch
    needs: save-image-info
    runs-on: ubuntu-latest
    steps:
      - name: 📦 Checkout dev branch
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN2 }}
          ref: dev
          fetch-depth: 0

      - name: 🔍 Verify file exists in dev
        id: check-file
        run: |
          if [ -f ".github/deployment-info/qa-image-info.json" ]; then
            echo "✅ Image info file found in dev branch"
            echo "file_exists=true" >> $GITHUB_OUTPUT
            cat .github/deployment-info/qa-image-info.json | jq '.'
          else
            echo "⚠️  Image info file not found in dev branch"
            echo "file_exists=false" >> $GITHUB_OUTPUT
          fi

      - name: 📤 Copy file to current branch
        if: steps.check-file.outputs.file_exists == 'true'
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📤 COPYING IMAGE INFO TO CURRENT BRANCH"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          
          IMAGE_INFO=$(cat .github/deployment-info/qa-image-info.json)
          mkdir -p .github/deployment-info
          echo "${IMAGE_INFO}" > .github/deployment-info/qa-image-info.json
          
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add .github/deployment-info/qa-image-info.json
          
          if git diff --staged --quiet; then
            echo "⏭️  No changes to commit in current branch"
          else
            IMAGE_TAG=$(echo "${IMAGE_INFO}" | jq -r '.image_tag')
            FULL_IMAGE=$(echo "${IMAGE_INFO}" | jq -r '.full_image')
            
            git commit -m "🐳 Update QA image info - ${IMAGE_TAG}" \
              -m "Image: ${FULL_IMAGE}" \
              -m "Branch: ${{ github.ref_name }}" \
              -m "Triggered by: ${{ github.actor }}"
            
            git push origin ${{ github.ref_name }}
            echo "✅ Image info pushed to current branch"
            echo ""
            echo "📊 File location: .github/deployment-info/qa-image-info.json"
            echo "🌿 Branch: ${{ github.ref_name }}"
          fi
          
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              

  # ───────────────────────────────────────────────────────────
  # Job FINAL: Resumen completo del workflow
  # ───────────────────────────────────────────────────────────
  workflow-summary:
    name: 📊 Workflow Summary
    needs: [test, analyze-queries, merge-to-dev, create-pr-to-integration, build-image, save-image-info]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Generate Workflow Summary
        run: |
          echo "# 🚀 Feature Workflow Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📊 Workflow Execution Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Status de cada job
          echo "| Job | Status | Details |" >> $GITHUB_STEP_SUMMARY
          echo "|-----|--------|---------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🧪 Tests | ${{ needs.test.result == 'success' && '✅ Passed' || '❌ Failed' }} | Unit & Integration Tests |" >> $GITHUB_STEP_SUMMARY
          echo "| 🔍 Query Analysis | ${{ needs.analyze-queries.result == 'success' && '✅ Completed' || '❌ Failed' }} | SQL Performance Analysis |" >> $GITHUB_STEP_SUMMARY
          echo "| 🔀 Merge to Dev | ${{ needs.merge-to-dev.result == 'success' && '✅ Merged' || '❌ Failed' }} | Feature → Dev |" >> $GITHUB_STEP_SUMMARY
          echo "| 📝 Create PR | ${{ needs.create-pr-to-integration.result == 'success' && '✅ Created' || '❌ Failed' }} | Dev → QA |" >> $GITHUB_STEP_SUMMARY
          echo "| 🐳 Build Image | ${{ needs.build-image.result == 'success' && '✅ Built' || '❌ Failed' }} | Docker Image |" >> $GITHUB_STEP_SUMMARY
          echo "| 💾 Save Info | ${{ needs.save-image-info.result == 'success' && '✅ Saved' || '❌ Failed' }} | Image Metadata |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Información de la imagen construida
          if [[ "${{ needs.build-image.result }}" == "success" ]]; then
            echo "## 🐳 Docker Image Information" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Property | Value |" >> $GITHUB_STEP_SUMMARY
            echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
            echo "| **Image Name** | \`mi-aplicacion\` |" >> $GITHUB_STEP_SUMMARY
            echo "| **Primary Tag** | \`${{ needs.build-image.outputs.image_tag }}\` |" >> $GITHUB_STEP_SUMMARY
            echo "| **Full Image** | \`${{ needs.build-image.outputs.full_image }}\` |" >> $GITHUB_STEP_SUMMARY
            echo "| **Commit SHA** | \`${{ needs.build-image.outputs.short_sha }}\` |" >> $GITHUB_STEP_SUMMARY
            echo "| **All Tags** | \`${{ needs.build-image.outputs.all_tags }}\` |" >> $GITHUB_STEP_SUMMARY
            echo "| **Environment** | \`qa\` |" >> $GITHUB_STEP_SUMMARY
            echo "| **Registry** | \`us-central1-docker.pkg.dev/elite-ceremony-476307-s9/dariasmoises979\` |" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            echo "### 🔗 Quick Access" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "- [View in GCP Console](https://console.cloud.google.com/artifacts/docker/elite-ceremony-476307-s9/us-central1/dariasmoises979/mi-aplicacion)" >> $GITHUB_STEP_SUMMARY
            echo "- Workflow Run: [\`${{ github.run_id }}\`](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            
            echo "### 📦 Pull Commands" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo '```bash' >> $GITHUB_STEP_SUMMARY
            echo "# Configure Docker for GCP" >> $GITHUB_STEP_SUMMARY
            echo "gcloud auth configure-docker us-central1-docker.pkg.dev" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "# Pull the image" >> $GITHUB_STEP_SUMMARY
            echo "docker pull ${{ needs.build-image.outputs.full_image }}" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
          fi
          
          # Metadata del workflow
          echo "## 📋 Workflow Metadata" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Property | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| **Source Branch** | \`${{ github.ref_name }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Triggered By** | @${{ github.actor }} |" >> $GITHUB_STEP_SUMMARY
          echo "| **Commit** | \`${{ github.sha }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| **Timestamp** | $(date -u +'%Y-%m-%d %H:%M:%S UTC') |" >> $GITHUB_STEP_SUMMARY
          echo "| **Repository** | ${{ github.repository }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Next steps
          echo "## 🎯 Next Steps" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [[ "${{ needs.build-image.result }}" == "success" ]]; then
            echo "1. ✅ **Image Ready**: The Docker image has been built and pushed to GCP Artifact Registry" >> $GITHUB_STEP_SUMMARY
            echo "2. 📝 **PR Created**: A pull request from \`dev\` to \`qa\` has been created" >> $GITHUB_STEP_SUMMARY
            echo "3. 💾 **Info Saved**: Image metadata saved to \`.github/deployment-info/qa-image-info.json\`" >> $GITHUB_STEP_SUMMARY
            echo "4. 🚀 **Ready for QA**: Once the PR is merged, QA workflow will automatically deploy using tag \`${{ needs.build-image.outputs.image_tag }}\`" >> $GITHUB_STEP_SUMMARY
          else
            echo "⚠️ **Action Required**: Some jobs failed. Please check the logs above." >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "_Generated by Feature Workflow - $(date -u +'%Y-%m-%d %H:%M:%S UTC')_" >> $GITHUB_STEP_SUMMARY

  say-hello:
    name: Confirmación de modificación independiente
    needs: create-pr-to-integration
    if: success()
    runs-on: ubuntu-latest
    steps:
      - name: Mensaje final
        run: |
          echo "✅ Se demuestra que se puede modificar el workflow por proyecto sin afectar a otros proyectos."
          echo "🐳 Image built: ${{ needs.build-image.outputs.full_image }}"
          echo "🏷️  Tag: ${{ needs.build-image.outputs.image_tag }}"